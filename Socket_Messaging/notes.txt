socket - lets python communicate over a network using TCP/UDP protocol. In this script, use TCP for guranteed packet delivery
csv - reads RPMI output .csv file 

HOST = "0.0.0.0" -- This means that is listens on all network interfaces. It becomes a server that any device on the network can connect to
PORT = 5000 a port is a communication channel. this is my own custom program, so i need a custom port. 

def read_rpmi_csv(filepath): --> this function simply opens the RPMI csv file and prints each row 

with open(filepath, 'r') as f:   with ensures the file autmatically closes 

-----STRUCTURE: ----- 

1. CSV manipulation and establishing a server 
Host handles:
CSV reading
laser detection
timestamps
data filtering
data aggregation

2. Commanding, visualization and control 
Client handles: 
commands
visualization
experiment control


1. Final OSI Model for the RPMI Socket System

OSI Layer	                          Implementation in Your System	                                                        Role
7. Application	       Custom Python socket protocol (START, STOP, PING, LASER_ON, LASER_OFF)	               Defines how the client and host communicate
6. Presentation	       UTF-8 string encoding, CSV formatting, timestamp conversion (UTC)	                   Formats and interprets data
5. Session	           Persistent TCP socket connection between client and host	                               Manages connection lifecycle
4. Transport	       TCP	                                                                                   Reliable ordered delivery of messages
3. Network	           IPv4	                                                                                   Routes packets between machines
2. Data Link	                   Ethernet (LAN)	                                                           Local network communication
1. Physical	           Ethernet cable / network hardware	                                                   Physical transmission of bits


----First Function----
def get_latest_csv(folder)

import os --> os gives python access to operating system functionality such as: 
os.listdir()	list files in a folder
os.path.getmtime()	get file modification time
os.path.join()	safely construct file paths

folder --> this is a function argument 
ex. get_latest_csv("C:/RPMI/output_files")
the function will search folder path you pass in 

os.listdir(folder) --> this will list everything in the directory 
[
"dlog_2023-08-09_1106.csv",
"dlog_2023-08-09_1110.csv",
"notes.txt",
"config.json"
]


if "file.csv".endswith(".csv"): 
this just means it is looking for files with a .csv file 
It will list files such as 
[
"dlog_2023-08-09_1106.csv",
"dlog_2023-08-09_1110.csv"
]

Now you need to sort by modification time -- files.sort(key=lambda x: os.path.getmtime(os.path.join(folder, x)))
file.sort() this normally sorts alphabettically, but here we have to add rules 
"key = " This tells python sort using this function instead of deafult alphabetical sorting 

lambda: This creates a small anonymous function so you don't have to fully write out the function 
for example this is the equivalent long form:
def get_time(x):
    return os.path.getmtime(os.path.join(folder, x))

os.path.join(folder, x): builds a full path 
Ex. 
folder = C:/RPMI/output_files
x = log1.csv
Result: C:/RPMI/output_files/log1.csv
We used this previously in the ontology section. This puts the second term wihting the folder path 


os.path.getmtime(): 
returns: file last modification timestamp 
the most recently modified file has the largest timestamp 
the function sorts from oldest to newest because the unix time number is greatest for newest 
since .sort() sorts in ascending order, the newest is always last

Returning lastest file: 
return os.path.join(folder, files[-1])
newest always last 


---Second Function--- 
def process_row(row, previous_laser_state) 

CSV file (RPMI writing)
        ↓
tail_csv()
        ↓
process_row()
        ↓
Laser ON/OFF detection
        ↓
timestamps printed in real time

Instead of analyzing the file after the run, detect events as the machine runs 


def process_row(row, previous_laser_state):
row --> one row from the CSV (as a dictionary)
previous_laser_state --> what the laser was doing before the current state 
output: returns the current laser_state so the next row can compare 

Step 1 Parse Timestamp 

Crash Safe Format:
try:
    # code that might cause an exception
    line1
    line2
    line3
except SomeException:
    # what to do if an exception occurs
    handle_exception()

try:
    ...
except Exception:
    return previous_laser_state
Many possible errors could happen here --> Missing columns (row["Laser On"]) doesn’t exist, empty rows, bad timestamp/format
Without try/except the program will crash but with it -- it can skip rows and such

timestamp = pd.to_datetime(row["TimeStamp"], errors="coerce")
string --> datetime object 
take values from timestamp individually and convert to datetime object 

except Exception:
return previous_laser_state 

General form to say: catch any error that happens in that try block and continue iterating


Step 2: Convert To UTC 
if timestamp.tzinfo is None:
    timestamp = timestamp.tz_localize("UTC")
.tzinfo = timezone info 
if it doesn't have a timezone assign it to UTC 

Step 3: Get Laser State 
laser_state = float(row["Laser On"])
this is int with either 0 or 1 
'
Step 4: Detect Transisitions 

if previous_laser_state == 0 and laser_state != 0:
laser was off and laser is now on then the event has started 

if previous_laser_state != 0 and laser_state == 0:
laser was ON and now is OFF --> stop event 

These are both if statements not elif becuase they are independent events


---Function 3----

def tail_csv(filepath):
Defines a function that takes the file path as input.

global running: 
uses a global variable "running" to control the loop 
allows you to stop monitoring from outside the funciton by setting running = False 

print("Monitoring CSV:", filepath )
console message to show which file is being monitored 

previous_laser_state = 0 
keeps track of the last known laser state 
0 = OFF, 1 = ON 

with open(filepath, "r") as f: 
Package --> built in python (open is part of Python core)
Purpose: Opens a file for reading "r" , writing "w" or appending "a" 
shorthand
"with" keyword --> context manager that automatically closes the file when done 
ensures no file handles are left open 
This is telling to open the file in read mode 
as f: 
f is just a variable that represents the file object 
you could call it anything (as file_obj) but as f is common 
example equivalence:
f = open("data.csv", "r")
# do stuff
f.close()

DictReader(f) --> reads rows as dictionaries instead of lists 
Keys = column names (header), Values = row values 
You can remember columns by name instead of the column order manually. 
Ex. csv.DictReader(f)
{'TimeStamp': '2025-03-19 12:00:00', 'Laser On': '0'}
{'TimeStamp': '2025-03-19 12:01:00', 'Laser On': '1'}

csv.reader(f)
['2025-03-19 12:00:00', '0']

for row in reader 
iterates over each existing row in CSV file 
row is a dictionary from DictReader that you iterate through each time 
previous_laser_state keeps track of whether the laser was ON and OFF in the previous row 

Real-time monitoring loop 
This loop runs continuously as long as the global variable running is True
Designed for real-time monitoring while RPMI writes new CSV rows

while running: 
This loop until something sets running = False 
Usually triggered by a manual interupt

position = f.tell() 
f.tell() gives the current file pointer position in bytes. Returns current position in the file 
used to remember where we are in the file before trying to read new lines 

line = f.readline() 
reads one line at a time from the file 
Moves the file pointer forward 
if there's no new line (end of file) it returns an empty string

if not line:
    time.sleep(0.2)   --- wait 0.2 seconds
    f.seek(position)  --- f.seek(position)


Processing a new line

line.strip().split(",")
Removes leading/trailing whitespace (strip)
Splits the CSV line into a list of values

Example:
line = "2025-03-19 12:01:00,1\n"
line.strip().split(",") → ['2025-03-19 12:01:00', '1']

previous_laser_state = process_row(row, previous_laser_state)
Passes the new row and previous state to the laser detection function
Updates previous_laser_state for the next iteration


What is a global variable? 
A global variable is a variable defined outside all functions accessible everywhere 
For example:
running = True #global variable, defined outside the function 
def stop(): 
    global running  #call the global variable
    running = False  #you have to call the global variable first then change logic 


---Function 4----
start_server() = main host program 
glocal running --> allows this function to control the loop inside tail_csv()

server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
This is creating a TCP server socket 
AF_INET --> IPv4 
SOCK_STREAM --> TCP 

Bind to address:
server.bind((HOST, PORT))
HOST = "0.0.0.0" → listen on all network interfaces
PORT = 5000 → communication port

Start listening for incoming connections: 
server.listen(1) 
Allows incoming connections 
1 = max number of queued connections 

Wait for client: 
conn, addr = server.accept() 
Blocking call --> waits until client connects 
conn --> communication socket with that client 
addr --> client's IP + port 
example output: Connected by ('192.168.1.5', 54321)

Main command loop: 
while True:  --> This loop keeps the server running forever, handling commands

Recieve data: 
data = conn.recv(1024).decode().strip()
conn.recv(1024) --> recieve up to 1024 bytes 
.decode()  --> convert bytes to string 
.strip()  --> remove whitespace/newlines 
b"START\n" → "START"

if not data: 
    break 
if client disconnects --> recv() returns empty 
breaks the loop and closes connection 

Command handling
if data == "START" 

latest_file = get_latest_csv(RPMI_FOLDER)
returns newest .csv file  

No file case, the file hasn't appeared yet 
if latest_file is None:
    print("No CSV files found")
    continue

Start Monitoring
running = True
tail_csv(latest_file)
the server is now stuck inside tail_csv() until it finishes 



STOP command
elif data == "STOP":
    print("Stopping monitoring")
    running = False

Sets running = False

This is supposed to stop:

while running:

inside tail_csv()
 But here’s the issue:

Because tail_csv() is blocking, this code:
 never executes while tail_csv is running



PING command
elif data == "PING":
    conn.send("ALIVE".encode())

Client sends "PING"
Server responds "ALIVE"
 Useful for:
connection testing, heartbeat checks



----Socket Client Code-----
Your Python code
    ↓
socket.send()
    ↓
TCP (Layer 4)
    ↓
IP packets (Layer 3)
    ↓
Ethernet frames (Layer 2)

conn.send("START".encode()) 
TCP turns that into: segments, packets, retransmissions. You never manually handle packets
You are sending bytes here, sockets only send bytes here 

UTF-8 
A way to convert text (characters) into bytes 
Example: 
"START".encode() --> b'START' --> Networks only transmit bytes not characters

Decode on socket host side 
data = conn.recv(1024).decode()
Converts bytes --> string 


How to actually run the system: 
Step 1: Start the server 
Run: python socket_host.py 
Output: Server waiting for connection... 
Server is running, listening on port 5000 Doing nothhing else 

Step 2: 
RPMI starts writing CSV files 
Your server is Not reading yet 

Step 3: Run the client 
python client.py 
client connects --> server prints connected by ('192.168.x.x', port) 

Step 4: Send START command 
From client: START 
Server side: 
Recieved --> START and then start RPMI monitoring 

Then: tail_csv(latest_file)